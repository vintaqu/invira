# Public assets

- `manifest.json` — PWA manifest
- `og-image.png` — Optional static OG image fallback (not required, dynamic OG is used instead)

Icons are generated dynamically via Next.js `src/app/icon.tsx` and `src/app/apple-icon.tsx`.
OG images are generated dynamically via `src/app/opengraph-image.tsx`.
